apiVersion: v1
kind: ConfigMap
metadata:
  name: grafana-alerting-config
  namespace: monitoring
  labels:
    app: grafana
data:
  contact-points.yaml: |
    apiVersion: 1
    contactPoints:
      - name: email-contact-point
        receivers:
          - uid: email-receiver
            type: email
            settings:
              addresses: "ashymanouski@student.com"
            disableResolveMessage: false
        disableProvenance: false

  alert-rules.yaml: |
    apiVersion: 1
    groups:
      - name: kubernetes-cluster-alerts
        folder: Kubernetes
        interval: 1m
        rules:
          - uid: high-cpu-utilization
            title: High CPU Utilization
            condition: A
            data:
              - refId: A
                queryType: ""
                relativeTimeRange:
                  from: 600
                  to: 0
                datasourceUid: prometheus
                model:
                  expr: 100 - (avg by (instance) (irate(node_cpu_seconds_total{mode="idle"}[5m])) * 100) > 80
                  intervalMs: 1000
                  maxDataPoints: 43200
            noDataState: NoData
            execErrState: Error
            for: 5m
            annotations:
              summary: High CPU utilization detected
              description: CPU utilization is above 80% for more than 5 minutes on node {{ $labels.instance }}
            labels:
              severity: warning
            isPaused: false

          - uid: high-memory-utilization
            title: High Memory Utilization
            condition: A
            data:
              - refId: A
                queryType: ""
                relativeTimeRange:
                  from: 600
                  to: 0
                datasourceUid: prometheus
                model:
                  expr: (1 - (node_memory_MemAvailable_bytes / node_memory_MemTotal_bytes)) * 100 > 85
                  intervalMs: 1000
                  maxDataPoints: 43200
            noDataState: NoData
            execErrState: Error
            for: 5m
            annotations:
              summary: High memory utilization detected
              description: Memory utilization is above 85% for more than 5 minutes on node {{ $labels.instance }}
            labels:
              severity: warning
            isPaused: false 